-- Default settings for glasses HUD

local GlassesConstants = {
    DEFAULT_COLOR = {1, 0, 0}, -- red
    DEFAULT_THROUGH = true,
    TICK_RATE = 0.5, -- seconds per update
}

return GlassesConstants
