local State = {}

-- Detected transposer tank sides
State.tanks = {
    -- example: { uuid = "uuid1", side = 4 },
}

-- Detected batteries (if you auto-detect more)
State.powerDevices = {
    -- example: { uuid = "uuidBB1" },
}

return State
